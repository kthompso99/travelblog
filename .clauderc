# Claude Code Project Configuration

## 🚨 CRITICAL: Configuration Management Rules

**BEFORE making ANY changes to file paths or directory structure:**

1. ✅ **READ** `docs/ARCHITECTURE.md` first
2. ✅ **NEVER** hardcode paths in scripts
3. ✅ **ALWAYS** use `lib/config-paths.js` for path constants
4. ✅ **EDIT** only `lib/config-paths.js` when changing structure
5. ✅ **TEST** all scripts after path changes:
   - `npm run validate`
   - `npm run build`
   - `npm run build:smart`

## 📁 Single Source of Truth

**ALL file paths must be imported from `lib/config-paths.js`**

```javascript
// ✅ CORRECT
const CONFIG = require('../lib/config-paths');
const tripPath = CONFIG.getTripConfigPath('greece');

// ❌ WRONG - DO NOT DO THIS
const tripPath = 'content/trips/greece/trip.json';
```

## 🔧 Key Files

- **`lib/config-paths.js`** - Path configuration (SINGLE SOURCE OF TRUTH)
- **`docs/ARCHITECTURE.md`** - Architecture guidelines (READ FIRST!)
- **`scripts/`** - All build/utility scripts import from config-paths.js

## 📝 When Making Changes

1. Structure changes → Edit `lib/config-paths.js` ONLY
2. New scripts → Import from `lib/config-paths.js`
3. Always follow checklist in `docs/ARCHITECTURE.md`

## ⚠️ Common Mistakes to Avoid

- ❌ Hardcoding `'content/trips'` in scripts
- ❌ Hardcoding `'trip.json'` or `'main.md'` filenames
- ❌ Creating duplicate path constants
- ❌ Not testing after path changes

## 📚 Required Reading

- `docs/ARCHITECTURE.md` - Configuration management
- `lib/config-paths.js` - Path definitions
- `README.md` - Project overview
